apiVersion: v1
kind: ConfigMap
metadata:
  name: gitlab-post-install-config-script
  annotations:
    helm.sh/hook: post-upgrade,post-install
data:
{{ (.Files.Glob "post-install-config.sh").AsConfig | indent 2 }}