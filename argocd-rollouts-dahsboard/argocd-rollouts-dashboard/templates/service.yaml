apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-rollouts-dashboard.fullname" . }}
  labels:
    {{- include "argocd-rollouts-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-rollouts-dashboard.selectorLabels" . | nindent 4 }}
